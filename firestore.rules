rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows any authenticated user to read and write to all data.
    // This is for development and should be made more secure for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
