rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read, write access to any document in any collection
    // if the user is authenticated.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
